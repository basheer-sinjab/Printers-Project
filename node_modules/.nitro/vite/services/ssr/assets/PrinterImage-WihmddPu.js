import { t as cn } from "./utils-0-yik925.js";
import { c as resolveImage } from "./pms-ALghGJ54.js";
import { jsx } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { Printer } from "lucide-react";
//#region src/components/PrinterImage.tsx
function PrinterImage({ path, alt, className }) {
	const { data } = useQuery({
		queryKey: ["img", path],
		queryFn: () => resolveImage(path),
		enabled: !!path,
		staleTime: 18e5
	});
	if (!path || !data) return /* @__PURE__ */ jsx("div", {
		className: cn("flex items-center justify-center bg-secondary text-muted-foreground", className),
		children: /* @__PURE__ */ jsx(Printer, { className: "size-10 opacity-40" })
	});
	return /* @__PURE__ */ jsx("img", {
		src: data,
		alt,
		loading: "lazy",
		className: cn("object-cover", className)
	});
}
//#endregion
export { PrinterImage as t };
